<Window x:Class="KeyNStroke.AnnotateLine"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KeyNStroke"
        mc:Ignorable="d"
        Title="AnnotateLine" Height="800" Width="800"
        AllowsTransparency="True"
        Topmost="True"
        ShowInTaskbar="False"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent" Loaded="Window_Loaded" Closed="Window_Closed">
    <Grid>
        <Canvas x:Name="drawingCanvas">
            <Polyline x:Name="drawingPolyline" Stroke="#FF7EEF84" StrokeThickness="4" StrokeLineJoin="Round" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                <Polyline.Effect>
                    <BlurEffect Radius="2" KernelType="Box"/>
                </Polyline.Effect>
            </Polyline>
        </Canvas>
    </Grid>
</Window>
